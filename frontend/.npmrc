# UniFlow — npm config (frontend)
#
# See ../.npmrc for the rationale. Frontend gets its own copy because the
# GitHub Actions workflow runs `npm ci` with `working-directory: frontend`,
# and we want the lenient resolution even if npm doesn't pick up the
# parent-directory .npmrc cleanly.
legacy-peer-deps=true
fund=false
audit=false
