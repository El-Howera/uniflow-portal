// Forms composer is shared across professor / ta / sa / admin — the page
// reads `localStorage.currentUserRole` to compute its navigation prefix.
export { default } from '../professor/FormComposer';
